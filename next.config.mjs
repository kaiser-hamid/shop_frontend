/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'picsum.photos',
            'images.unsplash.com',
            'res.cloudinary.com',
            'localhost',
            '127.0.0.1'
        ],
    },
};

export default nextConfig;
